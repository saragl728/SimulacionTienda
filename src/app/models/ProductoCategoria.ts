//clase para sacarlo en string
export class ProductoCategoria {
  producto: string;
  categoria: string;
  constructor(producto: string, categoria: string) {
    this.producto = producto;
    this.categoria = categoria;
  }
}
